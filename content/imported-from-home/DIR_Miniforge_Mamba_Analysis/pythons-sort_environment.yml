# Environment file for pythons-sort
# Generated automatically from Python import analysis
# Use: mamba env create -f pythons-sort_environment.yml
# Or: conda env create -f pythons-sort_environment.yml

name: pythons_sort
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # Most frequently used packages:

  # Additional packages via pip (if not available in conda):
  - pip:
