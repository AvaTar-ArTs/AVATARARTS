# Ollama Categories Pack (Intel Mac 16GB, CPU-only)

This pack provides per-category scripts to install/uninstall models and a ranking of top picks.

Usage:
```bash
chmod +x */install_*.sh */uninstall_*.sh
./general/install_general.sh
./coding/install_coding.sh
# ...
```
See rankings.csv for details.
