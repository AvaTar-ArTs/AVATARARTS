var Clock = "12h"; // choose between "12h" or "24h"
var Lang = "en"; 
var WeekdayColor = "#51627f"; // change the weekday color; enter hex code
var CalColor = "#51627f"; // change the month and date color; enter hex code
var WeatherColor = "#51627f"; // change the weather color; enter hex code
var LocationColor = "#51627f"; // change the location color; enter hex code
var ClockColor = "#51627f"; // change the clock color; enter hex code
