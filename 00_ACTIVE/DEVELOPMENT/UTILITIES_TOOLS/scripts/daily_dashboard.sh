#!/bin/bash
# Daily Dashboard - Your Morning Ritual

clear
echo "????????????????????????????????????????????????????????????????"
echo "           ?? STEVEN'S EMPIRE - DAILY DASHBOARD ??"
echo "????????????????????????????????????????????????????????????????"
echo ""
echo "?? $(date '+%A, %B %d, %Y - %I:%M %p')"
echo ""

# Load API keys
source ~/.env.d/loader.sh 2>/dev/null

# Check API status
echo "?? API Status:"
[ -n "$OPENAI_API_KEY" ] && echo "   ? OpenAI" || echo "   ? OpenAI (REQUIRED - get from platform.openai.com)"
[ -n "$TWILIO_ACCOUNT_SID" ] && echo "   ? Twilio" || echo "   ? Twilio (REQUIRED - get from twilio.com)"
[ -n "$ANTHROPIC_API_KEY" ] && echo "   ? Anthropic" || echo "   ??  Anthropic (recommended)"
[ -n "$GOOGLE_API_KEY" ] && echo "   ? Google Gemini" || echo "   ??  Gemini (recommended - FREE!)"
[ -n "$GROQ_API_KEY" ] && echo "   ? Groq" || echo "   ??  Groq (recommended - FREE!)"
[ -n "$ELEVENLABS_API_KEY" ] && echo "   ? ElevenLabs" || echo "   ??  ElevenLabs (for voice AI)"
echo ""

# Revenue tracking
echo "?? Current Revenue Status:"
echo "   HeavenlyHands: $30,000/month (LIVE)"
echo "   New Clients: $0/month"
echo "   ????????????????????????????????"
echo "   Total: $30,000/month"
echo "   Goal: $100,000/month by month end"
echo "   Gap: $70,000/month to close"
echo ""

# This week's goals
echo "?? This Week's Goals:"
echo "   [ ] Get missing API keys (if any above)"
echo "   [ ] Find 50 plumber prospects in Tampa"
echo "   [ ] Launch automated campaign"
echo "   [ ] Book 5+ demos"
echo "   [ ] Close 2-3 clients"
echo "   [ ] Add $12-24K/month revenue"
echo ""

# Project status
echo "?? Project Status:"
echo "   ? Universal Empire: Ready to launch"
echo "   ? CleanConnect Pro: Ready to launch"
echo "   ? Album AI: Needs Stripe integration (3 hours)"
echo "   ? Templates: Package and sell (2 days)"
echo ""

# Quick actions
echo "? Quick Actions:"
echo "   1. cd ~/workspace/work/universal-sales-empire/"
echo "   2. cd ~/workspace/work/cleanconnect-pro/"
echo "   3. cd ~/workspace/work/album-ai/"
echo "   4. cd ~/workspace/work/heavenlyhands/"
echo ""

# Today's focus
echo "?? TODAY'S FOCUS:"
if [ -z "$OPENAI_API_KEY" ] || [ -z "$TWILIO_ACCOUNT_SID" ]; then
  echo "   ? Get missing API keys FIRST!"
  echo "   ? Read: cat ~/workspace/docs/guides/API_KEYS_SETUP_GUIDE.md"
else
  echo "   ? Launch your first campaign!"
  echo "   ? cd ~/workspace/work/universal-sales-empire/"
fi

echo ""
echo "????????????????????????????????????????????????????????????????"
echo "              Let's build that $100K/month! ??"
echo "????????????????????????????????????????????????????????????????"
echo ""
