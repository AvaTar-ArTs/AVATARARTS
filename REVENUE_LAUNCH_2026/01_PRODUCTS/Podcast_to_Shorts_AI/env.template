# Podcast_to_Shorts_AI - Environment Template (SAFE)
#
# ✅ DO THIS:
# - Copy this file to `.env` (recommended) OR set environment variables directly.
# - Replace placeholder values (never commit real keys into your repo).
#
# Option A (recommended): copy to .env in this folder
#   cp env.template .env
#
# Option B: set env var in your terminal (Mac/Linux)
#   export OPENAI_API_KEY="sk-your-key-here"
#
# Option C: set env var for one command only (Mac/Linux)
#   OPENAI_API_KEY="sk-your-key-here" python3 podcast_to_shorts_ai_FIXED.py --audio your.mp3
#
# Windows (PowerShell):
#   $env:OPENAI_API_KEY="sk-your-key-here"
#
# Windows (cmd):
#   set OPENAI_API_KEY=sk-your-key-here
#

# Required: OpenAI API key
OPENAI_API_KEY=sk-your-key-here

# Optional: preferred model for text generation (defaults inside script if not set)
# Example: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Optional: override output folder (otherwise use CLI flag --output)
# OUTPUT_DIR=./output/podcast_to_shorts
